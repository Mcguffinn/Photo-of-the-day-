# NASA API Key
# Get your API key from https://api.nasa.gov/
REACT_APP_KEY=your_api_key_here

# If no key is provided, the application will use the DEMO_KEY,
# which is rate-limited to 30 requests per hour per IP. 